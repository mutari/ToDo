function Login(user) {
	if(!user) return
	this.getData = () => data
	this.getFrames = () => frames
	this.logOut = () => login = new Login()

	const data = {
		id: user.id,
		name: user.name,
		email: user.email,
	}
	const frames = user.frames.map(frame => ({
		id: frame.id,
		title: frame.id,
	}))
}